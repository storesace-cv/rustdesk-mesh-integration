# Copy this to deploy/.supabase-credentials and fill in values.
# This file is intended to be gitignored and kept on the deploy machine only.

# Supabase project reference (found in the Supabase dashboard)
SUPABASE_PROJECT_REF="your-project-ref"

# Supabase URL e.g. https://<ref>.supabase.co
SUPABASE_URL="https://your-ref.supabase.co"

# Service Role Key (keep secret)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Optional: SUPABASE_DB_URL used by psql to apply migrations
# SUPABASE_DB_URL="postgresql://..."
